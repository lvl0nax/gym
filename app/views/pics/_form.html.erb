<%= simple_form_for([@album, @picture]) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :alt %>
    <%= f.input :photo %>
    <%= f.input :description %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
<%= @album.name %>
